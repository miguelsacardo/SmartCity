import { Link } from "react-router"

export function HeaderDetail({ title , type }){
    {
        type === "sensor" &&(
            <div>
                <Link>
                    
                </Link>
            </div>
        )
    }
}